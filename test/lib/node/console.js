QUnit.test( "console.log", function(assert) {
    assert.ok(
        console.log instanceof Function,
        "Should be an instance of Function."
    );
});

QUnit.test( "console.info", function(assert) {
    assert.ok(
        console.info instanceof Function,
        "Should be an instance of Function."
    );
});

QUnit.test( "console.error", function(assert) {
    assert.ok(
        console.error instanceof Function,
        "Should be an instance of Function."
    );
});

QUnit.test( "console.warn", function(assert) {
    assert.ok(
        console.warn instanceof Function,
        "Should be an instance of Function."
    );
});

QUnit.test( "console.dir", function(assert) {
    assert.ok(
        console.dir instanceof Function,
        "Should be an instance of Function."
    );
});

QUnit.test( "console.time", function(assert) {
    assert.ok(
        console.time instanceof Function,
        "Should be an instance of Function."
    );
});

QUnit.test( "console.timeEnd", function(assert) {
    assert.ok(
        console.timeEnd instanceof Function,
        "Should be an instance of Function."
    );
});

QUnit.test( "console.trace", function(assert) {
    assert.ok(
        console.trace instanceof Function,
        "Should be an instance of Function."
    );
});

QUnit.test( "console.assert", function(assert) {
    assert.ok(
        console.assert instanceof Function,
        "Should be an instance of Function."
    );
});
